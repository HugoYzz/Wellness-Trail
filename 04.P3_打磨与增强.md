# 04. P3——打磨与增强

> 依据：00.健康管理网站架构方案.md §6.7（待办条）、§7.5（数据导出）、§10-P3
> 状态：已完成（2026-09-04）。P0-P3 主线全部收官。

## 一、交付物

| 层 | 文件 | 说明 |
|---|---|---|
| API | `server/app/routers/settings.py` | `/api/settings/usage`（Provider 信息 + token 用量 + 费用估算）、`/api/settings/export`（全量 JSON 备份下载） |
| 迁移 | `alembic/versions/10d493fadae4_message_token_breakdown.py` | messages 表增加 prompt/completion/cache_hit token 细分列 |
| 前端 | `web/src/components/TodoStrip.vue` | 顶栏常驻今日待办条（9 项 ✓/✗，点击跳聊天预填） |
| 前端 | `web/src/views/SettingsView.vue` | 设置页（Provider 卡 / 费用卡 / 一键备份导出） |
| 前端 | `web/src/stores/todo.ts` | Pinia store：待办状态 + 聊天预填（跨页通信） |
| 前端 | `web/src/views/ChatView.vue` | 历史会话 tab 切换 + 确认入账后待办条实时刷新 |

## 二、功能细节

### 待办条（§6.7 "打开即引导"落地）
- 9 项：晨重/夜重/奶茶/游泳/力量/步数/入睡/夜饿/补剂，与 chips、xlsx 高频列一一对应
- 完成判定：当日 records 按类型/槽位/运动 kind 匹配（体重分 am/evening，运动分 swim/strength）
- 点击未完成项 → 跳聊天页并预填该类型提示语（如「游了 分钟泳」）
- 确认入账后经 `kangji-refresh-todo` 自定义事件实时刷新，无需重载页面

### 设置页（§7.5 + §10-P3）
- Provider 卡：模型/接口/掩码 Key（明示密钥位置与更换方式，不做在线改 Key——避免明文经前端）
- 费用卡：按 DeepSeek 牌价估算（输入未命中 ¥2/M、命中 ¥0.2/M、输出 ¥8/M），token 细分自本次迁移后开始累计
- 备份导出：全量 JSON（36 records + 会话/消息 + KB 元数据 + 基线），文件名 `kangji-backup-YYYY-MM-DD.json`

### 历史会话（§7.1）
- 聊天页顶部按天 tab 列表，今天高亮；切换加载对应消息流
- 发送消息作用于当前选中会话（引擎按 chat_id 回放历史）

## 三、验收记录（2026-09-04，浏览器 7/7 PASS）

| # | 场景 | 结果 |
|---|---|---|
| 1 | 待办条全 9 项显示，晨重绿✓（今日有 106.4），跨页常驻 | PASS |
| 2 | 洞察页点「游泳」→ 跳聊天页，输入框预填「游了 分钟泳」 | PASS |
| 3 | 设置页三卡 + 导出下载 kangji-backup-2026-09-04.json（36 records） | PASS |
| 4 | 会话 tab「09-04（今天）」显示 | PASS |
| 5 | 发「今天走了9000步」→ 确认卡片 → 步数 ✗→✓ 实时变绿（无刷新） | PASS |
| 6 | 控制台零报错 | PASS |

截图：`data/backups/shot-p3-01~03`。验收数据（9000 步 + 测试会话）已清理。

## 四、偏差记录

0. **代码复查补丁（2026-09-04 收尾后）**：
   - `prompts.py` 模块级 `TODAY` 常量跨零点过期 → 改为请求时计算；
   - 动态上下文"近3日"实装 21 日全量（P2 扩窗口改漏）→ 摘要限定 3 日，21 日仅供异常信号；
   - 时间线删除/手动页新增记录后待办条不刷新 → 补发 `kangji-refresh-todo` 事件；
   - Dashboard 基线硬编码 → `/api/stats/trends` 新增 `baseline` 字段（读 users.settings_json，含冲刺目标区间），前端改为动态取用；
   - `_fmt_record` 补 step/waist/night_hunger/supplement 的格式化（此前 step 显示原始 dict）。
1. **Alembic autogenerate 误检 FTS5 影子表**（kb_fts_content 等 6 张）为"待删除表"——已手工从迁移中剔除（FTS 表由 ensure_fts 运行时管理，不入 schema 版本）。
2. **费用统计为估算口径**：牌价硬编码于 settings.py PRICE dict（元/百万 token），API 改价需改代码；token 细分自本迁移后累计，此前仅 total。
3. **时区坑**：前端今日日期用 `toISOString()` 会按 UTC 截断（凌晨差一天），已改本地时区拼装。
4. **WebPush / PWA / 语义检索 / 会话摘要压缩**（§10-P3 可选项）未做——单用户本地场景收益低，待真实使用反馈后再评估。

## 五、UI 排版专项优化（2026-09-04 夜，参考 Oura/Linear 暗色设计语言）

- **设计令牌重写**（style.css）：三层表面（bg/bg-soft/card）、语义色衍生 dim/glow、圆角/阴影/动效统一令牌；顶部微渐变背景、自定义滚动条、focus-visible 焦点环、tabular-nums 数字对齐
- **品牌标识**：蓝色渐变脉搏 SVG logo（顶栏 + 空状态），导航改胶囊样式、激活项蓝色高亮
- **待办条**：新增 n/9 进度徽章（蓝色→全完成变绿）；未完成项✗改·，降噪
- **聊天页**：空会话引导态（图标+示例话术）；输入区卡片化（chips+输入框+纸飞机发送按钮合一）；用户气泡蓝色渐变；气泡入场动画；移除 chips 里冗余的「时间线→」导航入口
- **洞察页**：KPI 数字 27px/字重 800，卡片渐变+悬停上浮；腰围无数据时显示虚线空状态（替换空壳卡片）；周报表行悬停、隔行无感化
- **时间线**：日期标题分隔线、类型标签蓝色底、删除按钮悬停才显示（触屏恒显）
- **确认卡片**：渐变背景+阴影+入场动画，警告条改琥珀色底色块
- **移动端**：<480px 收紧顶栏/导航/字号；聊天页高度适配

验收：5 页桌面 + 2 页手机（390px）全 PASS，无横向溢出、控制台零报错。

## 六、运行方式（不变）

```powershell
.\start.ps1        # 一键：后端 8001 + 前端 5173
```

## 七、P4 进展（上线与文档，§10-P4）

- [x] 版本化 JSON 备份、隔离恢复演练与事务恢复
- [x] 用户手册（日常话术示例）+ 项目 README
- [x] 安全清单（单用户登录、SQLite 文件权限、密钥管理）
- [x] GitHub Actions：后端、前端与浏览器级 SSE 断线续传验证
- [ ] 公网部署脚本（Caddy 反代 + HTTPS / Tailscale）

## 八、P3.1 个性化今日计划（2026-09-13）

- 原固定九项改为候选记录类型；系统按当前阶段日程、近 3 天身体状态、近 4 周同星期习惯和当日记录，生成 5–6 项今日计划。
- 首次生成须由用户确认；支持逐项“今日不安排”、恢复及“运动改休息”，结果保存在 `users.settings_json.daily_todo_overrides`，不新增数据库表。
- 状态拆为“稍后 / 待处理 / 已达成 / 已反馈 / 今日不安排”。“力量未做”、步数未达标、晚睡等属于“已反馈”，不再误显示为绿色完成；不适用项不进入进度分母。
- 奶茶与夜饿合并为晚间回顾，“0 杯 / 没饿”是有效完成；补剂同时核对维D3和锌，任意一条不再代表全部完成。
- 训练按主线阶段周计划出现；近 3 天腰/踝明显不适时，以身体状态复核替换训练，并保留线下就医边界。
- 顶部待办条收敛为“今日 n/m + 下一项”的跨页摘要；聊天页右栏作为唯一完整计划视图，移除右栏内重复的数据计数卡。
- API：`GET /api/todos/today` 生成/读取计划；`PATCH /api/todos/today` 处理确认、跳过、恢复、休息与重置。
