# 03. P2——洞察与回看

> 依据：00.健康管理网站架构方案.md §7.4（图表页）、§6.7（异常识别）、§10-P2
> 状态：已完成（2026-09-04）。验收口径：**趋势可见、异常可感知**。

## 一、交付物

| 层 | 文件 | 说明 |
|---|---|---|
| API | `server/app/routers/stats.py` | `/api/stats/trends`（全量趋势序列）+ `/api/stats/weekly`（周报聚合）；SQL 实时聚合不落派生表（§6.5-3） |
| 前端 | `web/src/views/DashboardView.vue` | 洞察页：KPI 卡 + 周报表 + 7 类 ECharts 图表 |
| 前端 | `web/src/api.ts` | getTrends + Trends 类型 |
| 引擎 | `server/app/services/chat_engine.py` | 新增 get_trend 工具（周报文本化回喂模型） |
| 提示 | `server/app/services/prompts.py` | 周复盘话术指引 + 异常信号检测注入动态上下文 |

## 二、图表清单（对齐 §10-P2）

| 图表 | 形式 | 口径 |
|---|---|---|
| 晨/夜体重 | 双折线（蓝实/紫虚）+ 基线 108.55 虚线 | am/evening 双槽 |
| 奶茶档位（戒断进度） | 阶梯线 + 面积 | 0杯=0 → 全糖=4 数值化，越低越好 |
| 步数 | 柱状 + 红虚线 8000 | plan §八：步数 ≥8000 |
| 入睡时间 | 折线 + 时间标签 | bedtime 原文展示 |
| 游泳时长 | 柱状 | duration_min 累计 |
| 腰围 | 折线 + 基线 110 | 数据不足时整图隐藏 |
| 夜饿处理 | 三色散点 | 没饿绿/加餐预案橙/破戒红 |

周报表复刻 xlsx"体重趋势"口径：**每周晨重 7 天均值、较上周、腰围、奶茶分布、步数日均、达标评价**（plan §5.2：每周 0.5-1.5kg 为达标）。

## 三、AI 复盘与异常识别（§6.7）

- **get_trend 工具**：模型收到"复盘一下这周"类问题 → 先调工具拿周报数据 → 对照达标口径给评价、点名奶茶/夜宵/步数/游泳、末尾给一句可执行的改进点。
- **异常信号**（注入每轮动态上下文，提及时注明 06-监测.md §二来源）：
  - 体重连续 2 周不变（≥15 天数据且首尾周均差 <0.3kg）→ 查隐性热量
  - 肠胃不适连续 ≥3 天（body site=肠胃）→ 提示就医
- 记录窗口由近 3 日扩至 **21 天**（异常检测需要长窗口），上下文摘要仍以近 3 日为主。

## 四、验收记录（2026-09-04）

| # | 场景 | 结果 |
|---|---|---|
| 1 | `/api/stats/trends` 数据核对 | PASS：晨重 5 点（106.4 今晨）、奶茶 4 档位、步数 4 日、周报聚合 106.88/5 天 |
| 2 | 浏览器洞察页（像素级检测） | PASS：7 图中 6 个渲染正常（腰围无数据按设计隐藏），KPI/周报表数值全对，控制台零错误 |
| 3 | 「复盘一下这周的情况」 | PASS：调 get_trend → 周报速览（-1.67kg vs 基线）+ 点名 9/1 全糖可乐连锁反应 + 步数偏静 + 游泳半程 + 下周改进点 |
| 4 | 前端构建 | PASS（echarts 全量打包 1.1MB/gzip 374KB，本地单用户可接受） |

截图：`data/backups/shot-dashboard-full.png`

## 五、偏差记录

1. **echarts 全量引入**未做按需裁剪（gzip 374KB）——本地应用优先交付速度，P3 若上公网再优化。
2. **异常信号中"精力下降"未实现**：records 无精力字段（方案中为自评 1-5 分，属新增记录类型），待后续需要时扩展 type。
3. **腰围图**在无数据时整块隐藏（而非空坐标架）——设计选择，避免空图误导。

## 六、P3 展望（下一步）

- 设置页：Provider 配置、JSON 全量备份导出（§7.5）
- 历史会话列表 UI + SSE 断线重连
- 待办条（顶栏"今日待办：晨重 ✓ / 夜重 ✗ …"，§6.7）
- 数据补充：腰围首测、精力自评字段
