# 01. P0——骨架与数据底座（已交付）

> 状态：**完成**（2026-09-04）
> 依据：`00.健康管理网站架构方案.md` v0.2 §10-P0
> 验收标准：可跑通"手动记录 → 时间线可见"；xlsx 真实 4 行全部导入且核对一致

## 一、交付物清单

| 类别 | 内容 | 位置 |
|---|---|---|
| 仓库骨架 | 目录结构（server/web/scripts/docs/data/assets）+ .gitignore + git init + 启动脚本 | `E:\康迹\`、`start.ps1` |
| 数据源副本 | 7 月 KB 11 个 md + 8 月主线 xlsx/docx（只读副本，源文件不动） | `assets\july_health\`、`assets\august_fatloss\` |
| 后端依赖 | venv + fastapi/uvicorn/sqlalchemy/alembic/pydantic/httpx/openpyxl/python-docx/sse-starlette | `server\.venv\`、`server\pyproject.toml` |
| 数据模型 | 7 表：users/records/chats/messages/kb_docs/kb_chunks/imports（records 11 类） | `server\app\models\__init__.py` |
| 首版迁移 | Alembic autogenerate + upgrade head（SQLite WAL、batch 模式） | `server\alembic\versions\271fb9261753_*.py` |
| 校验闸 | §2.3 全量：绝对范围/枚举合法/槽位规则（Pydantic v2）+ 体重相对 ±5kg warning（查库） | `server\app\schemas.py`、`server\app\routers\records.py` |
| API | GET/POST/PUT/DELETE /api/records（含 meta 枚举接口）、GET /api/health | `server\app\routers\records.py` |
| 迁移脚本 | import_xlsx.py（逐列映射+台账幂等+基线入 settings）、docx2md.py、import_kb.py、verify_import.py、acceptance_p0.py | `scripts\` |
| 前端骨架 | Vite+Vue3+TS+Pinia+Router，聊天壳（含快捷 chips 跳转）、时间线（按日分组/删除）、手动记录表单（11 类动态表单+枚举下拉） | `web\src\` |

## 二、数据迁移结果

- **records**：35 条（2026-08-31 ~ 09-03 共 4 天）——晨重/夜重 8、餐食 11、奶茶/甜饮 4、夜饿 1、运动 2、步数 4、睡眠 4、备注 1；source=import，raw_text 留存 xlsx 原文
- **自由文本→枚举推断**：I 列"一大杯可乐"→全糖、"无糖"系列→无糖；N 列"烤冷面…"→破戒（原文存 raw_text）
- **9-04（当天）xlsx 行不导入**（§8.2 只导今天之前），当日晨重 106.4 经手动表单走新站链路补录（record id=36）
- **kb_chunks**：143 块——august_fatloss 35（plan.docx 34 + xlsx 使用说明 1）、july_health 108（10 个文件按标题切块，section_no/标签齐全）
- **基线参数**：users.settings_json.baseline（108.55kg/110cm/两阶段/3周与长期目标），体重趋势公式值未导入
- **幂等**：import_xlsx 重跑 0 新增（台账跳过 4 行）；import_kb 重跑覆盖旧块

## 三、验收记录（scripts/acceptance_p0.py，9/9 PASS）

1. GET /api/health → 200 {app: 康迹}
2. 迁移 records 可见（35 条）
3. 日期覆盖 8-31~9-03
4. 奶茶档位推断：2026-09-01"一大杯可乐"→ 全糖
5. 手动添加今日晨重 106.4（slot=am, source=manual, raw_text 留痕）
6. 当日时间线立即可见（?date=2026-09-04 → 1 条）
7. 绝对闸：700kg → 422 拒绝
8. 枚举闸：奶茶档位"超多糖" → 422 拒绝
9. 相对闸：106.4→130kg（+23.6kg）不硬拒，返回 warning 供复核

verify_import.py 14/14 PASS（台账行数、分类型计数、晨重抽点 107.6/106.95/106.75/106.7）。

前端验证：`npm run build`（vue-tsc 0 错误）；dev 页面 title=康迹；Vite 代理 /api → 8000 联通。

## 四、运行方式

```powershell
# 一键启动（两个窗口：后端 8000 + 前端 5173）
E:\康迹\start.ps1
# 手机（同 WiFi）：http://172.21.15.34:5173

# 或手动：
cd E:\康迹\server ; .\.venv\Scripts\python.exe -m uvicorn app.main:app --port 8000
cd E:\康迹\web ; npm run dev

# 迁移重跑（幂等）：
cd E:\康迹 ; server\.venv\Scripts\python.exe scripts\import_xlsx.py
#            server\.venv\Scripts\python.exe scripts\docx2md.py ; scripts\import_kb.py
# 核对：      server\.venv\Scripts\python.exe scripts\verify_import.py
# 验收：      server\.venv\Scripts\python.exe scripts\acceptance_p0.py（需后端在跑）
```

## 五、与架构的偏差记录

1. **kb_docs 表新增**（§5 未列出）：支撑 §6.4 方案版本化（重导入覆盖旧块、保留版本戳）。
2. **7 月文件名勘误**：`04-日常锻炼.md`（v0.2 文档 §6.4 记作"04-日常饮食"类目，实际文件为"日常锻炼"），导入以实际文件为准。
3. **I/N 列为自由文本**（v0.2 §2.3 视作枚举下拉）：迁移脚本按规则推断档位，desc/raw_text 留存原文；P1 的 AI 抽取沿用同一推断口径。
4. records API 响应键名统一为 `fields`（ORM 列名 fields_json），前后端契约见 `web\src\api.ts`。

## 六、下一步（P1——AI 对话闭环）

- Provider 抽象（§6.2）+ DeepSeek 默认接入（需用户提供 API Key）
- SSE 事件契约 token/record_card/done（§6.1）
- register_record 工具 + 确认卡片组件（数值/槽位/枚举可改）
- FTS5 检索 + source_plan 分域 + 溯源约束（§6.4）
- 晨检/晚检引导（§6.7）
