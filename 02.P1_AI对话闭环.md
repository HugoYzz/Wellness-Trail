# 02. P1——AI 对话闭环

> 依据：00.健康管理网站架构方案.md §6（AI 对话引擎）、§7（前端）
> 状态：已完成（2026-09-04）。核心链路：**一句话 → 流式对话 → 确认卡片 → 入账 → 时间线可见**。

## 一、交付物

| 层 | 文件 | 说明 |
|---|---|---|
| 配置 | `server/.env` | DeepSeek API Key（已 gitignore，不入库） |
| 配置 | `server/app/config.py` | .env 加载 + DeepSeek 三要素 |
| Provider | `server/app/providers/base.py` | 抽象层：ProviderMessage / StreamEvent / ToolCallDelta |
| Provider | `server/app/providers/deepseek.py` | OpenAI 兼容协议，httpx 流式 + 工具调用增量聚合 |
| 检索 | `server/app/services/kb.py` | FTS5(trigram) + bm25 top-5 + source_plan 分域；<3 字符退化 LIKE |
| 提示 | `server/app/services/prompts.py` | 四层系统提示：人格/医疗红线 → 用户档案 → 近 3 日动态上下文 → KB 命中 |
| 引擎 | `server/app/services/chat_engine.py` | 工具轮次(≤3) + register_record 拦截 + 闸二校验 + 本地工具(get_kb/get_today_summary) |
| API | `server/app/routers/chat.py` | SSE 事件契约 token/record_card/done；chats/messages 持久化；confirm 入账回链 message.record_id |
| 前端 | `web/src/components/RecordCard.vue` | 可编辑确认卡（档位下拉/数值/日期），确认→入账 |
| 前端 | `web/src/views/ChatView.vue` | SSE 消费 + 打字机 + 卡片流 + 快捷 chips |
| 前端 | `web/src/api.ts` | sendMessageStream（fetch 流式解析 SSE） |
| 测试 | `scripts/test_chat.py` | 端到端脚本（SSE 消费 + --confirm 入账） |

## 二、关键设计落点

- **SSE 事件契约（§6.1）**：`token`（增量文本）、`record_card`（草稿卡+warnings，不入库）、`done`（usage/message_id/error）。
- **无卡片不入账**：模型口述"已记下"不产生任何副作用；唯一入账路径是 `POST /api/chats/{id}/confirm`。
- **判断重复的依据是数据不是话**：系统提示明确"唯一依据是动态上下文里的『今日已入账』清单，而非对话历史"。
- **知识库溯源**：get_kb 返回带 `source_plan + 文件名 + section_no` 的原文块，AI 回答需引用来源（实测维D问答引用了"7月健康知识库 · 01-激素平衡与抗雌化.md"）。
- **两代方案分域**：默认检索不混域；问"阶段/目标"走 august_fatloss，问"补剂/激素"走 july_health。
- **晨检引导（§6.7）**：当日首次建会话且晨重未记 → AI 开场引导称重。
- **工具轮次上限 3**：防模型与校验闸死循环；校验失败会回喂错误让模型自行修正。

## 三、验收记录（2026-09-04）

| # | 场景 | 结果 |
|---|---|---|
| 1 | "刚喝了杯三分糖奶茶，又游了45分钟泳" | PASS：2 张卡片（三分糖/游泳45min），AI 回应引用近 3 日对比 |
| 2 | "我该吃多少维D？" | PASS：2000IU 早餐后 + 知识库来源引用 |
| 3 | "我现在处于第几阶段？目标是什么" | PASS：冲刺期(8.31-9.20)/稳态期口径 + plan.docx 溯源 + 用今晨 106.4 算进度 |
| 4 | "今早空腹称了下，700公斤" | PASS：无卡片，模型主动要求复核（闸一在提示层生效） |
| 5 | 浏览器全流程（8 步） | PASS 7/8：流式回复、2 卡片、确认入账、时间线落账(09-04 3条)、维D问答；FAIL 1 项为发送按钮缺 @click（当场修复并回归通过） |
| 6 | 发送按钮回归 + 步数卡片 | PASS：点击发送有效；干净状态下步数消息正常出卡 |

## 四、偏差与修复记录

1. **端口 8000 → 8001**：本机有一个杀不掉的残留服务长占 8000（沙箱权限），后端整体迁到 8001；vite 代理、start.ps1、验收脚本同步更新。
2. **当前用户消息重复发送（真 bug）**：路由先落库用户消息，引擎组装历史时未排除 → 模型看到两遍。修复：`run_chat(user_msg_id=...)` + `_history_messages(before_id)` 排除本轮消息。
3. **"口头记账"幻觉（行为缺陷）**：模型有时只在文字里说"记下了"而不调工具（被对话历史里的旧说法误导）。两轮收紧系统提示：铁律"没有卡片就没有记录" + "判断重复只看今日已入账清单"。
4. **发送按钮无 @click**：浏览器验收发现，Enter 可发但点击无效。已补 `@click="send"` 并回归。
5. **验收中的一次误诊**：浏览器测试代理声称"后端把用户消息发了两遍"，经拦截 Provider payload 逐条核对——messages 无重复，实为测试代理自己重复发送污染了会话。清理测试数据后步数消息正常出卡。教训：对话历史会被"从未确认的口头记账"污染，设计上已靠清单判定兜底。

## 五、运行方式

```powershell
# 后端（server/ 下）
.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8001
# 前端（web/ 下）
npm run dev        # localhost:5173，/api 代理到 8001
# 或一键启动
.\start.ps1
# 端到端脚本（后端需在跑）
.venv\Scripts\python.exe ..\scripts\test_chat.py "今早空腹106.5"
```

## 六、P2 展望（下一步）

- 仪表盘（体重/腰围/步数趋势图，ECharts，§7.4）
- 数据导出（Markdown 周报）
- SSE 断线重连与会话切换 UI
- 提示词与检索参数调优（top-k、上下文窗口）
