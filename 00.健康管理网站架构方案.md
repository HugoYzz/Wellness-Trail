# 00. 康迹——健康管理网站架构方案

> 产品名：**康迹**（寓意：健康的轨迹；项目目录/仓库名 `康迹`，浏览器标签页标题、PWA 名称、图表水印统一用「康迹」）
> 项目路径：E:\康迹（原 F:\08.个人计划网站，2026-09 迁移）
> 文档状态：v0.2 架构修订稿（基于真实资产评审，配套 docs\架构评审与优化报告.md）
> 权威主线：8 月减脂作战方案（D:\桌面\Hugo\s-plan\plan.docx + 每日记录.xlsx）为当前主线；7 月健康管理计划（F:\01.健康管理\）作知识库背景
> 关联历史资产：F:\01.健康管理\（00~07 编号 md + 健康管理计划.md）、D:\桌面\Hugo\s-plan\（plan.docx + 每日记录.xlsx）；历史资产沿用 F 盘编号文档惯例；本项目（康迹）已迁至 E:\康迹

---

## 0. v0.2 修订说明（变更日志）

本版基于对真实资产（每日记录.xlsx、plan.docx、F:\01.健康管理\ 全部 md）的逐字段核对修订，完整评审证据见 `docs\架构评审与优化报告.md`。主要变更：

1. **锁定权威主线**：磁盘上并存"7 月广义健康管理计划"（三阶段、基线 100kg）与"8 月减脂作战方案"（两阶段、基线 108.55kg、血糖稳定化）两代方案，v0.1 曾混用两代概念。v0.2 裁定以 8 月方案为主线、7 月计划作 KB 背景（补剂/激素/中医/就医红线来源），并在 §6.4 用 `source_plan` 标签 + 系统提示锁定分域。
2. **重构数据字典（§2.3）**：按真实 xlsx 列（C~O）重写为 11 类记录；删除数据中不存在的虚构字段（food.stage/adherence、sleep.quality）；补上被漏掉的核心维度（奶茶/甜饮、夜饿处理、力量训练、腰围 cm）。
3. **修正知识库路径（§6.4/§8.1/§12）**：`D:\桌面\健康管理\`（不存在）→ `F:\01.健康管理\`；8 月方案源为 `plan.docx`（需 docx→md 且处理表格）。
4. **校准约束与示例（§1/§2.1/§2.3/§6.3）**：体重示例由失真的 72.3kg 改为真实量级（~107kg）；体重约束由"20–200kg"改为"绝对 40–200kg + 相对上次 ±5kg"双闸；补入睡跨零点归属规则。
5. **补齐实现契约（§6.1/§7.2）**：定义 SSE 事件契约（token/record_card/done）；确认卡从"数值+槽位"扩展为支持枚举下拉；闸二增加枚举合法性校验。
6. **对齐引导与监测（§6.5/§6.7/§7.3/§10）**：待办条、chips、异常识别对齐真实字段与 06-监测.md 的调整信号表。
7. **定名与迁移（2026-09-04）**：网站正式定名**康迹**；项目根由 `F:\08.个人计划网站` 迁移至 `E:\康迹`，本文档内项目路径同步更正（外部数据源 `F:\01.健康管理\`、`D:\桌面\Hugo\s-plan\` 路径不变，已核实仍在原位）。

广义健康维度（饮水量/血检/体征/疼痛疲劳评分/中医调理/习惯/体态照片）列为 P3 可选，不入 v0.2 主线数据模型。

---

## 1. 项目定位与目标

一句话定位：**康迹——以 AI 对话为驱动、个人记录为核心功能的单用户健康管理 Web 应用**。

目标拆解：

1. **记录零门槛**——晨重、夜重、三餐、加餐、奶茶/甜饮、游泳、力量、步数、入睡、夜饿、补剂等记录，通过自然语言一句话完成入账（「今早空腹 106.8，昨晚 00:30 睡」），由 AI 抽取结构化数据并交给用户确认，而不是打开表单逐格填写。
2. **方案落地闭环**——把既有的减脂作战方案（8 月主线）与健康管理 md（7 月背景：补剂 VitD3+锌、监测与就医红线、行动清单）变成 AI 的"知识底座"，AI 的建议必须可溯源到方案原文与章节号，杜绝脱离既定方案的随机发挥，也不混用两代方案的阶段与基线。
3. **趋势可读**——体重、腰围、奶茶档位、睡眠、运动等指标自动沉淀为图表与复盘，AI 按需解读趋势、识别异常（对齐 06-监测.md 的调整信号，如"体重连续 2 周不变"）。
4. **历史不丢**——迁移现有 xlsx 与 md/docx 数据作为基线，与未来的记录形成连续时间线。

约束前提：单用户（本人）、健康类数据敏感、不替代医疗诊断、预算敏感（API 费用可控）。

---

## 2. 产品形态

### 2.1 核心使用流程（聊天为主入口）

```
用户：今早空腹 106.8kg，昨晚 00:30 睡的
        │
        ▼
[聊天界面] ──消息──▶ 后端对话引擎
        │                │ 1. 组装上下文（档案+近况+知识库）
        │                │ 2. 调用 AI Provider
        │                │ 3. 模型返回"记录意图 + 结构化 JSON"
        │                │ 4. Pydantic 校验（字段、单位、范围）
        │                ▼
        ◀── 渲染"记录确认卡片" ──┐
        │（可编辑，显示解析出的        │ 校验失败→要求模型修正/放弃
        │  晨重 106.8 / 入睡 00:30）   │
        ▼
用户点 [确认入账]
        │
        ▼
   写库 records（source=chat，
   留存 raw_text 原文可追溯）
        │
        ▼
AI 追加一句回应（如"较昨日 -0.15kg，本周周均 107，趋势健康，继续保持"）
```

关键设计决策：**AI 永不直接写库，只产出"待确认记录"**。这是全系统的信用控制核心——AI 存在数值幻觉可能，任何自动入账都必须经过用户确认卡片这一步，且数据库留存用户原始语句（raw_text）以便事后核对。

### 2.2 信息架构（五个视图）

| 视图 | 定位 | 内容 |
|---|---|---|
| 对话 Chat | 主入口，默认页 | 消息流 + 记录确认卡片 + 快捷记录 chips |
| 时间线 Timeline | 记录回看 | 日历视图逐日展开已入账记录，可改可删 |
| 图表 Charts | 趋势洞察 | 晨/夜重曲线、周均值、游泳时长、步数、入睡时间分布 |
| 方案 Knowledge | 知识底座 | 现有 md 方案的结构化浏览 + 标注来源的 AI 问答 |
| 设置 Settings | 数据管理 | Provider 配置、导出/导入备份、目标参数（体重目标等） |

移动端优先做响应式（晨晚记录大概率在手机上完成），桌面端增强图表展示。

### 2.3 记录域定义（数据字典）

字段严格对齐真实 `每日记录.xlsx` 的列（C~O），并按 7 月 KB 补齐补剂/腰围/监测维度；"时间槽"区分晨重/夜重与三餐/加餐：

| 记录类型 type | 关键字段 | 真实来源 |
|---|---|---|
| weight | date, slot(am/evening), kg, note | xlsx C 晨重 / D 夜重 |
| sleep | date, bedtime(几点躺下) | xlsx M 入睡时间（原 quality 系虚构，已删） |
| meal | date, slot(breakfast/lunch/dinner/snack), desc | xlsx E 早餐 / F 午餐 / G 晚餐 / H 加餐 |
| sweet_drink | date, level(0杯/无糖/三分糖/半糖/全糖), desc | xlsx I 奶茶/甜饮（方案 3.4 戒断路线图核心指标） |
| night_hunger | date, level(没饿/加餐预案/破戒) | xlsx N 夜饿处理 |
| exercise | date, kind(swim/strength), duration_min(swim), done(strength bool) | xlsx J 游泳(分钟) / K 力量训练(完成/未做) |
| step | date, steps | xlsx L 步数 |
| waist | date(每周日), cm | xlsx 体重趋势 E 腰围（110→102-104 目标） |
| supplement | date, item(vitd3/zinc/copper…), dose, timing(早餐后/晚餐后), taken | 01-激素平衡.md §三（VitD3 2000IU / 锌 15-25mg） |
| body | date, site(腰/踝/肠胃), level, symptom | 06-监测.md（肠胃连续 3 天触发就医） |
| note | date, text（备注兜底，不入结构化分析） | xlsx O 备注 |

约束闸（对抗 AI 幻觉的第二道闸，均带单位）：

- **体重**：绝对 40–200kg + **相对上次确认值 ±5kg 双闸**；超相对闸不硬拒，确认卡标红要求复核（真实基线约 107kg，宽松绝对范围抓不住"10.7 误输/单位错"）。
- **入睡 bedtime**：存为该夜所属日的时刻；`bedtime < 12:00` 视为跨零点（次日凌晨）但仍归属记录日——AI 解析"昨晚 X 点睡"且 X<12 时，date 归到前一日。
- **枚举字段**：sweet_drink.level、night_hunger.level、exercise.done、supplement.timing/item 必须∈枚举集，非法值退回模型修正。
- **其余数值**：游泳 0–300min、步数 0–50000、腰围 50–200cm。

> P3（不入 v0.2 主线模型，仅 KB 可问答）：饮水量、血检（睾酮/雌二醇/维D/SHBG/25OHD）、体征（胸部脂肪/晨勃/精力）、疼痛疲劳评分 1-10、中医调理、习惯养成、体态照片——均来自 7 月广义健康管理 KB。

---

## 3. 总体架构

```
┌─────────────────────┐
│  前端 Web (Vite+Vue3) │  Chat 界面 / 确认卡片 / 图表 / 设置
└──────────┬──────────┘
           │ HTTPS / JSON + SSE 流式
┌──────────▼──────────┐
│  后端 API (FastAPI)  │  鉴权 / 对话引擎 / 记录服务 / 知识库检索
└──┬──────┬───────┬───┘
   │      │       │
┌──▼──┐ ┌─▼─────┐ ┌▼────────────┐
│SQLite│ │知识库 │ │AI Provider 层│
│(数据)│ │FTS5   │ │(多供应商抽象) │
└─────┘ └───────┘ └─────┬───────┘
                         │ OpenAI 兼容协议
              ┌──────────┼──────────┐
              ▼          ▼          ▼
        DeepSeek(默认)  通义/GLM/Kimi  Ollama(本地离线)
```

分层职责：

- **前端**：纯静态 SPA，无业务逻辑，只做渲染与交互。聊天消息通过 SSE 流式接收；确认卡片渲染模型返回的结构化 JSON。
- **后端**：唯一业务中枢。对话引擎负责上下文组装与工具调用；记录服务负责校验与写库；知识库服务负责方案检索。
- **数据层**：SQLite 起步（单用户数据量在 MB 级，SQLite 绰绰有余且零运维、单文件易备份），SQLAlchemy 抽象保留换 PostgreSQL 的余地。
- **AI 层**：统一 OpenAI 兼容协议（DeepSeek、通义、GLM、Kimi、Ollama 均原生兼容该协议），Provider 抽象实际上只有 base_url + api_key + model + 能力位（是否支持 function calling）四要素，切换成本极低。

---

## 4. 技术选型与理由

| 层 | 选型 | 理由 |
|---|---|---|
| 前端框架 | Vite + Vue 3 + TypeScript | 中文生态与文档最友好；个人维护期长，Vue SFC 结构直观；TS 约束数据结构 |
| 状态/路由 | Pinia + Vue Router | 标准搭配，体积小 |
| 图表 | ECharts | 中文文档成熟，体重趋势/分布图开箱即用 |
| UI | 手写 CSS（暗色聊天风可选）+ 少量组件 | 界面以聊天与卡片为主，无重型组件库必要；避免引入复杂依赖 |
| 后端 | FastAPI + Pydantic v2 | Python 是你最熟的语言；Pydantic 天然承担"AI 输出校验"职责（字段/类型/范围约束即声明式 schema） |
| ORM | SQLAlchemy 2.x | 模型与迁移管理，预留换库空间 |
| 流式 | SSE（FastAPI StreamingResponse） | 对话打字机效果；比 WebSocket 简单可靠 |
| 数据库 | SQLite + WAL | 单用户首选；FTS5 全文检索顺带解决知识库检索 |
| AI 客户端 | 自写薄 Adapter（httpx） | 供应商切换 + 日志 + 熔断可控，不绑 SDK |
| 种子迁移 | openpyxl（xlsx）+ python-docx/mammoth（docx 段落+表格）+ markdown 解析 | 你已有 openpyxl 经验；plan.docx 表密集，需完整转表格 |

否决项：不引重型 UI 框架（Element/AntD）、不做微服务、不引向量数据库（见 6.4，知识库规模小，FTS5 足够，语义检索列为 P3 可选增强）。

---

## 5. 数据模型（核心表）

```
users        — 单用户预留（id, username, password_hash, settings_json）
             settings_json 含：目标体重、Provider 配置、提醒偏好

records      — 全部记录（唯一事实源）
             id, type(weight/sleep/meal/sweet_drink/night_hunger/exercise/
                      step/waist/supplement/body/note),  ← 11 类，详见 §2.3
             date, slot, fields_json(结构化值，按 type 各有 schema),
             raw_text(用户原话, 溯源用), source(chat/manual/import),
             confirmed(bool), created_at
             索引: (date, type)

chats        — 会话（聊天页按天开新会话）
             id, date, title(自动生成如"9月4日晨间复盘"), summary(长会话压缩摘要)

messages     — 消息
             id, chat_id, role(user/assistant/tool), content,
             record_id(关联到经确认产生的 records，可跳转溯源),
             token_usage, created_at

kb_chunks    — 知识库块（7 月 md 直读 + 8 月 docx 转换后切块）
             id, doc_id, title(来源文件名), heading(所在章节),
             source_plan(august_fatloss/july_health), section_no(3.4/5.2/6.1…),
             content, tags(补剂/菜单/行动/监测/就医), created_at

imports      — 迁移台账（记录 xlsx 哪些 sheet+行已导入，幂等；跳过预填空行与公式错误值）
```

关键约定：

1. `records.raw_text` 与 `source` 必填——任何记录都可追溯到"人话原文 / 手动 / 迁移"，可审计。
2. 修改记录走"新建 + 软删"或留 audit 字段即可（个人应用，做轻）。
3. 图表统计全部由 SQL 实时聚合，不落派生表（数据量小，保持单一事实源）。
4. **粒度**：meal 按 meal-slot 各存一条（早/午/晚/加餐独立可确认）；sweet_drink 独立成类，以便"戒断路线图"档位趋势分析。
5. `kb_chunks.source_plan` 是两代方案分域的关键——检索与溯源时据此区分主线（8 月）与背景（7 月），杜绝混用基线与阶段。

---

## 6. AI 对话子系统设计（核心）

### 6.1 请求管线（每次用户消息的处理链）

```
① 组装静态系统提示（人格+边界+工具说明）
② 注入用户档案层（真实画像：108.55 基线/血糖稳定化核心/腰突护腰/踝弃跳/
   奶茶夜宵零食三座大山/补剂 VitD3+锌；阶段以 8 月主线为准）
③ 注入动态上下文层（近 3 日记录摘要 + 当日已入账清单，按 type+slot 去重防重复入账）
④ 知识库检索（按消息语义取 top-5 块，见 6.4）
⑤ 调用 Provider（流式 SSE），模型可发起工具调用
   └─ 工具集（function calling）：
       register_record(按 type 抽取结构化记录) / query_records / get_trend /
       get_kb / get_today_summary
⑥ register_record 返回 → Pydantic 校验（数值范围 + 相对合理性 + 枚举合法 + 跨零点日期）
   → 未通过则告知模型修正
⑦ 校验通过 → 前端渲染确认卡片（此时不入库）；一句话含多类型则拆多卡排队
⑧ 用户确认 → 入库 → 下一轮上下文已含该记录
```

工具调用采用 **OpenAI function calling 标准格式**；若某供应商不支持 tools（如部分轻量模型），降级方案为"约束 JSON 输出 + 正则兜底解析"，架构上按能力位切换。

**SSE 事件契约**（前后端约定，P1 落地）：流式响应分三类事件——`event: token`（增量文本，打字机效果）、`event: record_card`（一张待确认记录卡的结构化 JSON，一句话可触发多张、前端排队渲染）、`event: done`（本轮结束，附 token_usage 与费用估算）。register_record 的工具调用在后端拦截转为 record_card 事件，模型原始工具报文不直接暴露给前端。

### 6.2 Provider 抽象（多供应商）

```python
class AIProvider(ABC):
    base_url: str        # 各自兼容端点
    api_key: str
    model: str
    supports_tools: bool # 能力位
    async def chat(messages, tools=None, stream=True) -> Stream
```

- 默认 DeepSeek（你已有 Key、成本低、function calling 稳定）。
- 配置存 `users.settings_json`，设置页切换，前端零改动。
- 每轮记录 token 用量与费用估算，设置页展示（成本可视，防失控）。
- 失败熔断：超时/限流 → 明确报错提示，不静默降级；本地 Ollama 不可用时不影响主链路。

### 6.3 结构化抽取与确认机制（信用控制三重闸）

| 闸 | 手段 | 防什么 |
|---|---|---|
| 一 | 模型输出 JSON Schema 强约束（register_record 工具参数定义） | 格式乱、字段缺失 |
| 二 | 后端 Pydantic 校验：数值范围 + **相对上次 ±5kg 合理性** + **枚举合法性**（奶茶档位/夜饿/力量）+ 跨零点日期归属 | 数值幻觉、单位错、枚举乱填、日期错挂 |
| 三 | 用户确认卡片（数值可改 + 枚举下拉可改 + 槽位可改）+ raw_text 留痕 | 任何漏网之鱼可当场改/拒 |

设计纪律：**确认前系统状态不变**（只读会话），确认才写库；被拒的记录进入消息上下文供模型学习纠偏（"下次把夜重记到 evening 槽"）。

### 6.4 知识库（方案问答，RAG-lite）

- 源（真实路径）：
  - 7 月背景 KB = `F:\01.健康管理\` 的 00~07 编号 md（00 总览与阶段划分 ～ 07 第一周行动清单）+ `健康管理计划.md`（完整版）+ `补充.md`——已是 md，直接切块。
  - 8 月主线方案 = `D:\桌面\Hugo\s-plan\plan.docx`——需 docx→md（见 §8.1，须处理表格）。
- 两代分域（权威性控制核心）：每块打 `source_plan`。系统提示锁定——**减脂执行/阶段/目标/血糖稳定化以 8 月 plan.docx 为准；补剂剂量/激素/中医/就医红线以 7 月 KB 为准**（8 月 docx 无补剂细节）。禁止用 7 月的三阶段/100kg 基线解释 8 月记录，反之亦然。
- 切块：按文档标题层级切块（每块 ≈ 一个完整小节），入库 `kb_chunks`，记 `section_no`（3.4 戒断/5.2 预期/5.3 平台期/6.1 低血糖/6.2 腰/6.3 踝），打标签（补剂/菜单/行动/监测/就医）。
- 检索：**SQLite FTS5 中文全文检索（bm25）取 top-5**。知识库总量约几万字（7 月 md ~66KB + 8 月 docx 文本），模型上下文完全装得下，全文检索已够准，不必上向量库。
- 溯源约束：系统提示强制——"涉及补剂剂量、菜单/阶段、行动建议、监测信号时，必须引用知识库原文并注明来源文件名 + 章节号；知识库没有的内容不得编造，须明说'方案里没有，我帮你记下来待补充'或建议就医"。
- 方案版本化：源文件更新后重新导入覆盖并保留版本时间戳；`source_plan` + 版本戳共同保证 AI 对齐"正确那一代的最新版"。

### 6.5 上下文管理与成本控制

- 单轮上下文 = 静态提示（~1k tokens）+ 档案层（~0.5k，真实画像 + 8 月主线阶段）+ 动态上下文（近 3 日摘要 ~0.5–1k）+ 知识库命中 top-5（~1.5–2k）+ 会话消息窗口。目标**单轮 ≤ 5k tokens 量级**，单日重度使用成本预估在分毛级。
- 长会话：保留最近 20 条消息 + 历史滚动压缩为 `chats.summary`（触发阈值如 >40 条时调用模型总结一次）。
- 重复入账防护（按 type+slot 去重）：动态上下文含"当日已入账清单"，模型把"今早 106.8"识别为重复时提示"今日晨重已记录 106.8，是否要修改？"；同理区分晨重/夜重、早/午/晚/加餐、游泳/力量等各槽位。

### 6.6 安全与边界（医疗红线）

系统提示固定声明并在前端页脚常驻提示：

1. 本应用为记录与生活方式管理工具，**不做疾病诊断、不提供处方级建议**；
2. 症状持续/加重（腰突、踝、肠胃异常）话术一律"建议线下就医"，对齐 06-监测.md §三就医提醒与 plan.docx 6.1 低血糖（立即吃糖并告知）/6.2 腰/6.3 踝；肠胃不适连续 3 天以上提示就医；
3. 补剂剂量建议仅能引用 01-激素平衡.md §三原文（VitD3 2000IU 早餐后 / 锌 15-25mg 晚餐后 / 长期补锌 +1-2mg 铜）；方案外剂量、以及方案明列"暂不推荐"的蛋白粉（肠胃敏感期）/肌酸/促睾补剂，一律拒答并提示就医/问诊；
4. 涉及中药方剂，提示"先经中医师面诊确认体质"（06-监测.md），不自行给方。

### 6.7 主动引导（不做后台推送，做"打开即引导"）

- 晨检 Check-in：当日首次打开站点且晨重未记录 → 会话首条由 AI 发起"早，昨晚睡得怎么样？今早空腹多少？"，引导式对话驱动记录完成。
- 晚检：晚间记录缺失（夜重/游泳/力量/步数/奶茶/夜饿/入睡）时同类引导。
- 待办条：顶栏常驻"今日待办：晨重 ✓ / 夜重 ✗ / 奶茶 ✗ / 游泳 ✗ / 力量 ✗ / 步数 ✗ / 入睡 ✗ / 夜饿 ✗ / 补剂 ✗"，逐项点击可跳转聊天快捷输入。
- 异常识别（对齐 06-监测.md §二调整信号，替换 v0.1 单一"连续 3 日晨重上升"）：体重连续 2 周不变→提示查隐性热量（奶茶/夜宵/零食回潮）；精力明显下降→提示热量缺口可能过大；肠胃不适连续 3 天→提示就医；触发话术均注明来源。
- 定时 WebPush 列为 P3 可选增强（需浏览器常开或 PWA），不阻塞主线。

---

## 7. 前端设计要点

1. 默认落地页即聊天（历史会话按天列表 + 新会话入口）。
2. 记录确认卡片是核心组件：以"一卡一条记录"渲染模型 JSON，字段可编辑——数值可改、槽位可改（晨/夜重、早/午/晚/加餐）、**枚举下拉可改**（奶茶档位、夜饿处理、力量完成/未做、补剂 timing）、跨零点入睡时间可改；[确认入账] / [不要了] 两键；一句话含多类型（如晨重+入睡）拆多卡排队确认。
3. 快捷记录 chips：会话输入框上方常驻「记晨重」「记夜重」「奶茶/甜饮」「游泳」「力量」「步数」「入睡」「夜饿」「补剂」「不适」等 chip（对齐真实高频字段），点击预填该类型提示语（降低打字成本，实测此类输入以口语短句为主）。
4. 图表页与时间线页直接复用 records 事实源，支持"从某日聊天跳转该日记录"；图表页复刻 xlsx"体重趋势"周报口径（周平均体重、较上周、腰围曲线、达标评价），并新增奶茶档位趋势（戒断进度）、夜饿分布、游泳/力量频次。
5. 隐私：全站 HTTPS 部署；页面不接任何第三方统计/字体 CDN 之外的请求；数据导出按钮一键 JSON 全量备份。
6. 暗色优先的聊天式视觉 + 明确的信息层级，体重趋势等关键数字卡片突出显示。

---

## 8. 种子数据与历史迁移

### 8.1 导入内容

**逐列映射**（`每日记录.xlsx` 的"每日记录"sheet，数据自第 3 行起）：

| xlsx 列 | 去向 records | 说明 |
|---|---|---|
| C 晨重 / D 夜重 | weight(slot=am/evening) | kg |
| E 早餐 / F 午餐 / G 晚餐 / H 加餐 | meal(slot=breakfast/lunch/dinner/snack) | 各 slot 一条，自由文本 desc |
| I 奶茶/甜饮 | sweet_drink(level) | 下拉档位映射枚举 |
| J 游泳(分钟) | exercise(kind=swim, duration_min) | |
| K 力量训练 | exercise(kind=strength, done) | 完成/未做→bool |
| L 步数 | step(steps) | |
| M 入睡时间 | sleep(bedtime) | 跨零点归属规则见 §2.3 |
| N 夜饿处理 | night_hunger(level) | 没饿/加餐预案/破戒 |
| O 备注 | note(text) | 兜底；含"出差"等特殊情况 |

**源与方式**：

| 来源 | 去向 | 方式 |
|---|---|---|
| `D:\桌面\Hugo\s-plan\每日记录.xlsx`（每日记录 sheet） | records（source=import） | scripts/import_xlsx.py：**只导真实数据行（当前 4 行，8-31~9-03），跳过预填未来空行**；sheet+行号记入 imports 台账，幂等可重跑 |
| xlsx"体重趋势"sheet | settings 基线参数 | **不导公式值（有 #DIV/0!）**；把 初始晨重 108.55 / 腰围 110 / 阶段目标 写入 settings；周平均/较上周由 SQL 实时算 |
| xlsx"使用说明"sheet | kb_chunks（口径约定） | 记录口径（奶茶档位对照 3.4、力量/夜饿下拉含义）作 KB 补充 |
| `F:\01.健康管理\` 00~07 md + 健康管理计划.md + 补充.md | kb_chunks（source_plan=july_health） | scripts/import_kb.py 按标题切块，md 直读 |
| `D:\桌面\Hugo\s-plan\plan.docx` | kb_chunks（source_plan=august_fatloss） | scripts/docx2md.py：**python-docx 遍历段落 + 表格**（表密集，纯段落提取会丢表），转 md 后切块，保留章节号锚点 |

### 8.2 基线口径

- 迁移只导**今天（2026-09-04）之前**的已发生记录；今天起走新站日常链路。当前真实数据仅 8-31~9-03 四天。
- 基线口径（写入 settings）：初始晨重 108.55kg（2026-08-30）、腰围 110cm；8 月主线两阶段——冲刺期 8.31-9.20、稳态期 9.21+；3 周(9.20)目标约 104-105kg(BMI 33.9-34.3)/腰围 107-108，长期约 93-95kg(BMI 30.4-31.0)/腰围 102-104。
- 9.21 开学转稳态期：知识库标注阶段切换规则，AI 在接近该日期时提示"即将进入稳态期，菜单与运动量将调整"。
- 迁移完成跑核对脚本（scripts/verify_import.py）：xlsx 真实行数 vs records 条数、晨重曲线抽点对比（107.6/106.95/106.75/106.7），确保基线无损。

---

## 9. 部署与备份

- 形态：后端 uvicorn + 前端 vite build 静态产物，同机部署，Caddy/Nginx 反代上 HTTPS（个人证书用 Caddy 自动签发最省事）。
- 承载：初期跑本机（局域网手机可访问）；若放公网，开单用户登录（静态账号 + bcrypt 密码，不做注册流程），此前先过一遍安全清单。
- 备份：SQLite 单文件每日定时复制到 data/backups/（保留 30 份）；Settings 提供一键导出全量 JSON（含知识库版本号），归档到网盘。
- 恢复：导入 JSON 重建 records/chats，幂等设计保证可重放。

---

## 10. 分阶段路线图

### P0 — 骨架与数据底座（验收：可跑通"手动记录 → 时间线可见"）
- 仓库初始化：server/（FastAPI）、web/（Vite+Vue3）、scripts/、docs/ 目录结构与 CI 无关的本地启动脚本
- SQLite 表结构（records 11 类 + kb_chunks 带 source_plan/section_no）+ SQLAlchemy 模型 + Alembic 首版迁移
- 迁移脚本：xlsx → records（逐列映射 §8.1、跳过空行/公式、含核对脚本 verify_import.py）、F:\01.健康管理 md → kb_chunks、plan.docx → docx2md → kb_chunks
- 前端：聊天壳（静态消息）、时间线日历页、记录手动表单（含枚举下拉）
- **验收**：xlsx 真实 4 行全部导入且核对一致（晨重抽点 107.6/106.95/106.75/106.7）；手动添加一条晨重当天即在时间线显示

### P1 — AI 对话闭环（验收：核心体验成立）
- Provider 抽象 + DeepSeek 默认接入 + SSE 流式（落实 §6.1 事件契约 token/record_card/done）
- register_record 工具（按 type schema）+ Pydantic 校验（数值 + 相对 ±5kg + 枚举合法 + 跨零点）+ 确认卡片组件（数值/槽位/枚举下拉可改）
- 知识库检索接入 + 两代 source_plan 分域 + 溯源约束（文件名 + 章节号）生效
- 晨检/晚检引导逻辑（打开即引导）
- **验收**：连续 3 天用聊天完成晨重/夜重/奶茶/游泳/力量/入睡/夜饿记录，全程无表单填写；故意报超范围体重（如 700kg）或相对上次 +30kg 被拦截；奶茶"一大杯可乐"被正确归到"全糖"档
- **验收**：问"我该吃多少维D"→ 引用 01-激素平衡.md §三原文（2000IU 早餐后）并注明来源；问"现在第几阶段"→ 以 8 月主线（冲刺期）作答，不混用 7 月三阶段

### P2 — 洞察与回看（验收：趋势可见、异常可感知）
- ECharts：晨/夜重双曲线、周均值、腰围曲线、入睡时间分布、游泳时长 + 力量频次、步数、**奶茶档位趋势（戒断进度）**、夜饿分布
- 周报复刻 xlsx"体重趋势"口径（周平均、较上周、腰围、达标评价对照方案 5.2）
- 复盘话术：模型查询 records + get_trend 工具生成周复盘
- 异常识别：对齐 06-监测.md §二调整信号（体重连续 2 周不变→查隐性热量 / 精力下降→缺口过大 / 肠胃连续 3 天→就医），替换单一"连续 3 日晨重上升"
- 时间线改删补功能完善
- **验收**：周复盘准确引用真实数据曲线，无编造数字；平台期信号能触发并给出溯源建议

### P3 — 打磨与增强
- 会话摘要压缩、长会话稳定性
- 待办条、快捷 chips 完整化、PWA（可选）、WebPush（可选）
- Provider 切换 UI + 费用统计展示
- 语义检索实验（本地 bge-small-zh 或 API 嵌入），对比 FTS5 收益再决定是否替换
- **广义健康维度（来自 7 月 KB，可选纳入记录模型）**：饮水量、血检（睾酮/雌二醇/维D/SHBG/25OHD）、体征（胸部脂肪/晨勃/精力）、疼痛疲劳评分 1-10、中医调理、习惯养成、体态照片——待主线稳定后评估

### P4 — 上线与文档
- 部署脚本（Caddy 反代 + HTTPS）、备份 cron、恢复演练
- 用户手册 md（含日常使用话术示例）+ 项目 README
- 安全清单过一遍（登录、SQLite 文件权限、密钥管理）

---

## 11. 约束、风险与对策

| 风险 | 对策 |
|---|---|
| AI 数值幻觉导致脏数据 | 三重闸：JSON Schema → Pydantic（绝对范围 + 相对上次 ±5kg + 枚举合法 + 跨零点）→ 确认卡片 + raw_text 溯源 |
| **两代方案混用（7 月三阶段/100kg vs 8 月两阶段/108.55kg）致 AI 阶段与基线错乱** | kb_chunks 打 source_plan 标签 + 系统提示锁定分域权威（减脂执行以 8 月为准、补剂/就医以 7 月为准）+ 禁止跨代套用基线 |
| AI 建议脱离既定方案/编造 | 溯源约束进系统提示 + 知识库版本化；方案外内容明确拒答 |
| 医疗边界越界（腰突/肠胃症状被"建议") | 固定红线话术"就医"（对齐 06-监测.md/plan.docx 6.x），不做自行诊疗；补剂仅引用 01-激素平衡.md 原文 |
| API 费用失控 | 单轮 token 预算 + 用量日志 + 费用可视化 + 支持切本地 Ollama |
| 数据丢失/损坏 | SQLite 每日备份 + 全量 JSON 导出 + 幂等导入可重放 |
| 公网暴露隐私 | 单用户强口令 + HTTPS + 默认局域网部署 |
| 迁移出错丢历史/导入脏值 | imports 台账幂等（sheet+行号）+ 核对脚本 + 只读源文件；跳过预填空行、不导公式错误值（#DIV/0!）、docx 表格完整转换 |
| 维护期依赖过重（框架升级等） | 依赖最小化原则；前端无组件库、后端无重量框架；Python/Node 各锁版本 |

## 12. 建议目录结构

```
E:\康迹\
├── 00.健康管理网站架构方案.md     ← 本文档（v0.2，网站名「康迹」）
├── 01.P0_骨架与数据底座.md         ← 各期任务指令文档（沿用编号惯例）
├── docs\                           ← 设计笔记
│   └── 架构评审与优化报告.md        ← v0.2 评审证据 + 分级问题清单
├── server\
│   ├── app\  (main.py, models/, routers/chat.py, providers/, services/)
│   ├── alembic\
│   └── pyproject.toml
├── web\
│   ├── src\  (views/Chat.vue, Timeline.vue, Charts.vue, Knowledge.vue,
│   │          components/RecordCard.vue, api/, stores/)
│   └── package.json
├── scripts\
│   ├── import_xlsx.py     ← 逐列映射(§8.1)、跳过空行/公式
│   ├── docx2md.py         ← plan.docx 段落+表格转 md
│   ├── import_kb.py       ← md 切块，打 source_plan/section_no
│   └── verify_import.py   ← 迁移核对
├── data\            ← 运行时生成，不入库（sqlite + backups）
└── assets\          ← 迁移数据源只读副本：F:\01.健康管理\*.md（7 月 KB）
                        + Hugo\s-plan\plan.docx + 每日记录.xlsx（8 月主线）
```

---

## 附：下一步建议

1. v0.2 已完成基于真实资产的评审修订（配套 `docs\架构评审与优化报告.md`）；请复核 §2.3 数据字典 11 类与 §6.4 两代方案分域是否符合预期；
2. 冻结技术选型（第 4 章，若对 Vue3 有顾虑可换 React，架构不变）；
3. 开启 P0，先出 `01.P0_骨架与数据底座.md` 任务指令，按编号惯例逐期推进（数据模型与迁移映射以本版 §2.3/§8.1 为准）。
